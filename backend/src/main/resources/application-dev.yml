spring:
  jpa:
    hibernate:
      ddl-auto: none

  liquibase:
    enabled: true
    # Kasutame sama H2 master-faili mis põhiprofiilgi
    change-log: classpath:db/changelog/master-h2.xml
    url: jdbc:h2:mem:aiinterview-db;DB_CLOSE_DELAY=-1;MODE=PostgreSQL;DATABASE_TO_LOWER=TRUE;DEFAULT_NULL_ORDERING=HIGH
